<?php
$config = array(
	'servers' => array(
<% node['beanstalk_console']['servers'].each do |server| %>
		<%= "'beanstalk://#{server}'" %>
<% end %>
	),
	'storage' => dirname(__FILE__) . DIRECTORY_SEPARATOR . 'storage.json',
	'version' => '1.6.1',
);
